export class Property {
    constructor({ id, idProperty, idOwner, name, address, price, images }) {
      this.id = id;
      this.idProperty = idProperty;
      this.idOwner = idOwner;
      this.name = name;
      this.address = address;
      this.price = price;
      this.images = images;
    }
  }
  