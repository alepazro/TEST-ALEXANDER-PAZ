export class Image {
    constructor({ id, idPropertyImage, idProperty, file, enable }) {
      this.id = id;
      this.idPropertyImage = idPropertyImage;
      this.idProperty = idProperty;
      this.file = file;
      this.enable = enable;
    }
  }
  